{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local quota island.",
  "windows": ["main", "panel"],
  "permissions": ["core:default", "opener:default"]
}
