# 当前任务

- 状态：已完成
- 目标：完成 macOS 本地部署与 Universal 2 打包，并修复动态岛视觉、交互、沉浸模式及额度刷新可靠性。
- 结果：开启 Tauri `app.macOSPrivateApi`、使用 `NSStatusWindowLevel` 并去除胶囊浅色内描边后，详情层级不再被普通窗口盖住，常驻胶囊按显示器 work area 顶部避开物理刘海，周围白色矩形已消失；macOS 鼠标命中区域已收紧到可见胶囊，并增加失焦收回兜底。macOS 沉浸模式现改为检测前台应用窗口是否完整覆盖实际 236×46 胶囊区域，不再要求原生全屏；普通最大化以及手动覆盖胶囊的窗口都会触发，窗口恢复或移开后退出。悬停展开无法折叠的问题确认是构建包与安装包双实例重叠，构建脚本已统一覆盖启动 `/Applications/Codex Island.app` 并校验仅有一个进程。额度自动刷新已从 WebView 计时器迁移到 Rust 原生后台任务，支持固定轮询、失败指数退避、睡眠唤醒后按墙上时间补刷新以及详情页强制刷新。Universal 2 应用已重新安装，并验证跨过完整轮询周期后胶囊 `43%` 与服务端 `43%` 一致。本地构建使用 ad-hoc 签名，公开分发仍需 Developer ID 与公证。
